# Docker Access

Docker CLI and Docker Compose are available, connected to the host's Docker daemon via `/var/run/docker.sock`.

Commands like `docker ps`, `docker build`, `docker run`, and `docker compose` affect the host's Docker environment.
