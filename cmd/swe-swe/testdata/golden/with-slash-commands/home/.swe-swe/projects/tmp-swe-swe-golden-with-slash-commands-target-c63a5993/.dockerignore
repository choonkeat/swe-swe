# Exclude runtime directories from Docker build context
# These are mounted as volumes at runtime, not needed during build

# VSCode/code-server home directory (extensions, settings, history)
home/

# Enterprise certificates (mounted at runtime)
certs/

# Pre-built binaries (if any)
bin/

# Project path marker (runtime only)
.path

# Environment variables (runtime only)
.env

# Unix sockets (can't be added to tar anyway)
*.sock
